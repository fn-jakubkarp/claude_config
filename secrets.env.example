# Copy to secrets.env and fill in real values, then run install.sh.
# secrets.env is gitignored and must NEVER be committed.
# install.sh sources this and injects the values into the MCP config (~/.claude.json, user scope).

# 21st.dev "magic" MCP server API key — https://21st.dev
MAGIC_API_KEY=
